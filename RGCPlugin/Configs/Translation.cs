using System;

namespace RGCPlugin.Configs
{
    public class Translation
    {
        public string TranslationCode { get; private set; } = "en"; // Indicates which language code the translation uses
    }
}
